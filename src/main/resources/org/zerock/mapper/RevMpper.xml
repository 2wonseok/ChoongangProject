<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.Rev_BoardMapper">

	<insert id="insertSelectKey">
		<selectKey keyProperty="rev_seq" order="BEFORE"
		resultType="int">
			SELECT rev_seq.nextval FROM dual
		</selectKey>
		INSERT INTO tbl_reviewboard (rev_seq, rev_category, rev_title, rev_writer,
		rev_readcnt, rev_content, rev_filename, rev_good, rev_hate, rev_regdate, rev_updatedate)
		VALUES (#{rev.seq}, #{rev_category}, #{rev_title}, #{rev_writer}, 0, #{rev_content}, #{rev_filename},
		0, 0, sysdate, sysdate)
	</insert>

</mapper>