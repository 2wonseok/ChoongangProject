<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.qaboard.mapper.ReplyMapper">

	<!-- 
	DB 테이블 구조
	reply_seq NUMBER(10) PRIMARY KEY,
	reply_content VARCHAR2(3000) NOT NULL,
	reply_writer VARCHAR2(30) NOT NULL,
	reply_regdate DATE,
	reply_updatedate DATE,
	reply_boardname VARCHAR2(10) NOT NULL
	
	C (CREATE (INSERT) 처리) 
	R (READ (SELECT) 처리)
	U (UPDATE 처리)
	D (DELETE 처리)
	-->
	
	<insert id="insert">
		INSERT INTO tbl_qaboard (reply_seq, reply_boardname, reply_boardseq, reply_content, reply_writer, reply_regdate) 
		VALUES (reply_seq.nextval, #{reply_boardname}, #{reply_boardseq}, #{reply_content}, #{reply_wirter}, #{reply_regdate})
	</insert>
	
	<select id="read" resultType="org.zerock.qaboard.domain.ReplyVO">
		SELECT * FROM tbl_reply WHERE reply_boardname = #{reply_boardname} AND reply_seq = #{reply_seq}
	</select>
	
	<delete id="delete">
		DELETE FROM tbl_reply WHERE reply_boardname = #{reply_boardname} AND reply_seq= #{reply_seq}
	</delete>
	
	<update id="update">
		UPDATE tbl_reply SET reply_content = #{reply_content}, reply_updatedate = SYSDATE WHERE reply_boardname = #{reply_boardname} AND reply_seq = #{reply_seq}
	</update>
	
	<select id="getListWithPaging" resultType="org.zerock.qaboard.domain.ReplyVO">
		SELECT reply_seq, reply_boardname, reply_boardseq, reply_content, reply_writer, reply_regdate, reply_updatedate 
		FROM tbl_reply 
		WHERE reply_boardseq = #{reply_boardseq}
		ORDER BY reply_seq ASC
	</select>
</mapper>